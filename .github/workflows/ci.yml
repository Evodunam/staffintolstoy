name: CI

on:
  push:
  pull_request:
  workflow_dispatch:

concurrency:
  group: ci-${{ github.workflow }}-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: npm

      - name: Install
        run: npm ci

      - name: Typecheck + runtime guard tests
        run: npm run test:ci:runtime-guards

      - name: SEO smoke test
        run: npm run seo:smoke:ci

      - name: SEO mode smoke (shuffled + sequential)
        run: npm run seo:smoke:all-modes

      - name: SEO phased rollout smoke
        run: npm run seo:smoke:all-phases

      - name: Unit tests (client)
        run: npm test

      - name: Unit tests (server + shared)
        run: npm run test:server

      - name: Production build
        run: npm run build
