import { describe, it, expect } from "vitest";
import {
  MAX_UPLOAD_BYTES,
  assertMaxUploadSize,
  compressImageIfNeeded,
} from "./image-compression";

describe("image-compression", () => {
  describe("MAX_UPLOAD_BYTES", () => {
    it("is 10 GB", () => {
      const tenGB = 10 * 1024 * 1024 * 1024;
      expect(MAX_UPLOAD_BYTES).toBe(tenGB);
    });
  });

  describe("assertMaxUploadSize", () => {
    it("throws when file size exceeds 10 GB", () => {
      const overLimit = { name: "big.txt", size: MAX_UPLOAD_BYTES + 1 } as File;
      expect(() => assertMaxUploadSize(overLimit)).toThrow(
        /exceeds the maximum upload size of 10 GB/
      );
    });

    it("does not throw when file size equals 10 GB", () => {
      const atLimit = { name: "at-limit.txt", size: MAX_UPLOAD_BYTES } as File;
      expect(() => assertMaxUploadSize(atLimit)).not.toThrow();
    });

    it("does not throw when file size is under 10 GB", () => {
      const under = new File(["x"], "small.txt");
      expect(() => assertMaxUploadSize(under)).not.toThrow();
    });

    it("error message includes file name", () => {
      const over = { name: "my-photo.jpg", size: MAX_UPLOAD_BYTES + 1 } as File;
      expect(() => assertMaxUploadSize(over)).toThrow(/my-photo\.jpg/);
    });
  });

  describe("compressImageIfNeeded", () => {
    it("returns same file for non-image types", async () => {
      const pdf = new File(["%PDF-1.4"], "doc.pdf", {
        type: "application/pdf",
      });
      const result = await compressImageIfNeeded(pdf);
      expect(result).toBe(pdf);
      expect(result.name).toBe("doc.pdf");
      expect(result.size).toBe(pdf.size);
    });

    it("returns same file for text/plain", async () => {
      const txt = new File(["hello"], "readme.txt", { type: "text/plain" });
      const result = await compressImageIfNeeded(txt);
      expect(result).toBe(txt);
    });

    it("returns same file for image/gif (not in compressible list)", async () => {
      const gif = new File(["GIF89a"], "anim.gif", { type: "image/gif" });
      const result = await compressImageIfNeeded(gif);
      expect(result).toBe(gif);
    });

    // In happy-dom, Image + blob URL may not fire onload; test times out. Run in browser for full coverage.
    it.skip("returns a File for image/jpeg (compression or fallback)", async () => {
      // Tiny valid JPEG (1x1 pixel)
      const jpegBytes = new Uint8Array([
        ,
      ]);
      const jpegFile = new File([jpegBytes], "photo.jpg", {
        type: "image/jpeg",
      });
      const result = await compressImageIfNeeded(jpegFile);
      expect(result).toBeInstanceOf(File);
      expect(result.name).toMatch(/^photo\.(jpg|jpeg)$/);
      expect(result.type).toMatch(/^image\/(jpeg|webp)$/);
    });

    it.skip("returns a File for image/png (compression or fallback)", async () => {
      const pngBase64 =
        "iVBORw0KGgoAAAANSUhEUgAAAAEAAAABCAYAAAAfFcSJAAAADUlEQVR42mP8z8BQDwAEhQGAhKmMIQAAAABJRU5ErkJggg==";
      const pngBytes = Uint8Array.from(atob(pngBase64), (c) => c.charCodeAt(0));
      const pngFile = new File([pngBytes], "dot.png", { type: "image/png" });
      const result = await compressImageIfNeeded(pngFile);
      expect(result).toBeInstanceOf(File);
      expect(result.name).toBe("dot.png");
      expect(["image/png", "image/jpeg", "image/webp"]).toContain(result.type);
    });

    it.skip("returns a File for image/webp (compression or fallback)", async () => {
      const webpFile = new File(["RIFF....WEBP"], "pic.webp", {
        type: "image/webp",
      });
      const result = await compressImageIfNeeded(webpFile);
      expect(result).toBeInstanceOf(File);
      expect(result.name).toBe("pic.webp");
    });
  });
});
